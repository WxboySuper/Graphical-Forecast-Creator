## Summary

- Fix JS-0045 across monitor hooks and related pages: consistent returns on early useEffect/async exits and explicit trailing returns where required.
- Fix storm report utilities: move parseArchiveStormReportCsv above callers, use for-of in stormReportCsv, and simplify archive parsing loop.
- Remove unused collectionHasGeometry / isPolygonalGeometry exports from nwsAlerts.ts; drop unused useEffect import in useMonitorNwsAlerts.ts.
- Move radarSites import to top of file (module order).

Documentation and style DeepSource findings are intentionally deferred (gated separately for v1.6).

## Changelog

- Fixed DeepSource JS-0045 return consistency in monitor hooks, storm report CSV/parser utilities, and related pages.

## Test plan

- [x] stormReportCsv + stormReportParser unit tests
- [x] useFirestoreSleepRecovery, MonitorPage, nwsAlerts unit tests
- [ ] DeepSource check green on this PR
- [ ] Merge to beta and re-run checks on beta to main promotion PR (#400)
