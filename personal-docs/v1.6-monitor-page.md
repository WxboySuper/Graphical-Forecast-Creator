# v1.6 Monitor Page — Internal Progress Tracker

> **Local only.** This file lives under `personal-docs/`, which is gitignored (`*/personal-docs/*` in `.gitignore`). Do not commit.

**Last updated:** 2026-05-21  
**Feature branch:** `feature/v1-6-monitor-page`  
**Active worktree:** `C:\Users\super\.codex\worktrees\21e4\Graphical-Forecast-Creator`  
**Latest commit:** `feat: add Monitor page with live WMS layers and outlook overlay` (rebased onto `origin/beta`)  
**Product scope:** Monitor is a **standalone page/tab** at `/monitor` (not a Home replacement — intentional)

---

## Feature intent (as implemented so far)

Real-time **Monitor** workspace at `/monitor` — fifth main nav tab (after Home, Forecast, Discussion, Verification).

| In scope (built) | Out of scope / not started |
|------------------|----------------------------|
| OpenLayers map + pan/zoom persistence | Replacing or redesigning **Home** (`/`) as “front page” |
| Opt-in MRMS CONUS + site radar WMS | NWS/SPC official outlook layers |
| Opt-in GOES satellite WMS | Editing outlooks on this page |
| Read-only personal outlook overlay (current / local / cloud Day 1) | Animation loop (UI exists, map does not use it) |
| Local settings + premium cloud sync of `monitorSettings` | Committed branch / PR |

**v1.6 scope (confirmed):** Monitor stays a **standalone tab** — no Home replacement required.

---

## Work location

| Workspace | Path | Has monitor code? |
|-----------|------|-------------------|
| Cursor (default) | `Graphical-Forecast-Creator` on `main` | **No** |
| Feature worktree | `C:\Users\super\.codex\worktrees\21e4\Graphical-Forecast-Creator` | **Yes** (all WIP) |

### Uncommitted inventory (worktree only)

**Modified (12):**

- `e2e/smoke.spec.ts` — monitor smoke test (390×844, overflow check)
- `src/App.tsx` — route + import
- `src/App.test.tsx`, `src/AppPerformance.test.tsx`
- `src/auth/AuthProvider.tsx`, `src/auth/AuthProvider.test.tsx` — `monitorSettings` in sync doc
- `src/components/Layout/AppLayout.tsx` — `⌃4` → `/monitor`
- `src/components/Layout/NavbarSections.tsx`, `NavbarSections.test.tsx`
- `src/pages/ForecastPage.test.tsx`, `src/pages/index.ts`
- `src/store/index.ts` — `monitor` reducer

**Untracked (new feature):**

- `src/pages/MonitorPage.tsx`, `MonitorPage.css`
- `src/components/Monitor/` — `MonitorControls`, `MonitorMap`, tests, `index.ts`
- `src/monitor/` — `types`, `wms`, `storage`, `outlookSources`, tests
- `src/store/monitorSlice.ts`

---

## Progress checklist

### Done (functional skeleton)

- [x] Redux slice + normalized `MonitorSettings` + localStorage read/write
- [x] `AuthProvider` settings document includes `monitorSettings` (hydrate + debounced premium sync on page)
- [x] Route `/monitor`, navbar tab, keyboard shortcut `⌃4`
- [x] Control panel: radar mode/product/site, satellite product, opacities, outlook source picker
- [x] WMS layer builders (MRMS CONUS, site radar, GOES) + latest-time fetch
- [x] `MonitorMap`: base map, WMS layers, read-only vector outlook styling (reuses forecast OL styles)
- [x] Cloud cycle outlook load for premium users
- [x] Responsive layout CSS (portrait stack, landscape side panel)
- [x] Unit tests: `MonitorControls`, `monitorSettings`, `outlookSources`, `wms` (passing in worktree, 2026-05-21)

### Incomplete / known gaps

- [x] **WMS animation** — `useLiveWmsLayers` loops latest WMS frames; `MonitorMap` updates `TIME` param without recreating sources
- [x] **MonitorPage test** — render/wiring test with mocked map + live layers hook
- [x] **Rebase onto current `beta`** — clean rebase completed 2026-05-21
- [x] **First commit** — feature committed on `feature/v1-6-monitor-page` in worktree
- [ ] **MonitorMap unit tests** — OL map still untested (heavy mock)
- [ ] **E2E verified** — smoke test written; Playwright run not recorded in this doc
- [ ] **Manual QA** — live WMS on real network, site radar site list UX, cloud outlook edge cases
- [ ] **Coverage ≥80%** on new files — run coverage pass before PR
- [ ] **Push branch / open PR** — local only so far

### Quality bar (high standard — treat as release gate)

Use this as the definition of “done,” aligned with `AGENTS.md` and forecast-editor expectations.

| Area | Requirement | Status |
|------|-------------|--------|
| **Correctness** | Radar/satellite/outlook toggles match settings; map view persists; premium sync does not fight local edits | Partial — needs manual + test proof |
| **Animation** | Play loops latest WMS times (or documented frame strategy); pause cleans up; speed slider affects interval | Implemented — needs manual QA on live NOAA WMS |
| **Resilience** | WMS failures toast once; no runaway refetch; cloud outlook errors surfaced in control meta | Partial |
| **Auth/sync** | Free: localStorage only; premium signed-in: debounced Firestore sync; sign-out does not corrupt state | Implemented — needs regression tests |
| **Mobile** | No horizontal page overflow; map remains primary viewport; controls reachable without dual-axis scroll in one surface | CSS present — **needs Playwright + phone pass** |
| **Landscape phone** | Usable map + controls when height constrained | CSS present — **needs device check** |
| **A11y** | Labels on map/controls; keyboard path to key toggles where feasible | Partial — audit pass needed |
| **Tests** | Focused unit tests for all non-trivial modules; page-level test for hydrate/sync wiring; ≥80% on touched files | **Gap** on page/map/animation |
| **E2E** | `e2e/smoke.spec.ts` monitor case green in CI | Written, not verified here |
| **Performance** | No duplicate OL map init; layer swap does not leak listeners | Code review — `MonitorMap` mount effect deps look minimal |
| **Docs/changelog** | User-facing note for v1.6 when shipping | Not started |

---

## Known issues & risks

1. **All work is uncommitted** in the Codex worktree. Deleting the worktree loses the feature unless stashed/copied first.

2. **Branch base is old** (~80 commits behind `beta`). Rebasing will be non-trivial (Sentry, analytics, account page, home CSS, mobile forecast editor, etc. all landed since `09cb5aa`).

3. **Animation is misleading UX** — users can press Play with no map effect. Either implement or hide/disable until ready.

4. **External WMS dependency** — `opengeo.ncep.noaa.gov`, `nowcoast.noaa.gov`; CORS/network outages only covered by toasts, not retries/backoff.

5. **Site radar** — free-text site ID (`KTLX`); no picker/validation UX beyond regex in `normalizeMonitorSettings`.

6. **Outlook scope** — Day 1 only for cloud cycles “today”; status strings in UI — confirm product copy.

7. **Two workspaces confusion** — editing in main repo will not show monitor files; always use worktree path until ported.

8. **No remote branch guarantee** — `feature/v1-6-monitor-page` may not exist on `origin` with this WIP; confirm before relying on another machine.

---

## Revival plan (get it going again)

Ordered for minimum thrash and your quality bar.

### Phase 0 — Preserve work (30 min)

1. Open worktree: `C:\Users\super\.codex\worktrees\21e4\Graphical-Forecast-Creator`
2. Review `git status` and `git diff --stat`
3. Either:
   - **Option A:** Commit WIP on branch as `wip: monitor page scaffold (unverified)` and push to origin, or
   - **Option B:** `git diff > monitor-wip.patch` + copy untracked dirs to a safe backup
4. Do not delete worktree until backup exists

### Phase 1 — Modernize base (1–2 hrs)

1. `git fetch origin`
2. `git rebase origin/beta` (or merge `beta` if rebase is too painful — prefer rebase for clean PR)
3. Resolve conflicts prioritizing: `AuthProvider`, `App.tsx`, `NavbarSections`, `store/index.ts`, `e2e/smoke.spec.ts`
4. `pnpm install` if lockfile moved
5. `pnpm run build` — must pass

### Phase 2 — Close functional gaps (core product)

1. **Animation:** Implement in `MonitorMap` or a small hook:
   - Fetch time dimension / recent frames for active WMS layers
   - Interval from `animationSpeedMs`; respect `animationEnabled`
   - Pause on layer change / unmount
2. **Decide Home integration** (if v1.6 “front page” means more than a tab):
   - CTA on Home → Monitor, and/or
   - Default post-login route, and/or
   - Embedded mini-map preview
3. Manual pass: radar on/off, satellite on/off, each outlook source type, premium cloud cycle

### Phase 3 — Quality hardening (your standard)

1. Add `MonitorPage.test.tsx` — hydrate from localStorage + synced settings mock
2. Add `MonitorMap.test.tsx` — layer visibility / outlook feature count (mock OL or shallow where possible)
3. Extend `AuthProvider.test.tsx` — monitor settings equality in sync payload
4. Run coverage on touched paths; bring new files **≥80%**
5. Run Playwright:  
   `pnpm exec playwright test e2e/smoke.spec.ts -g monitor`  
   Plus portrait + landscape viewports if not already covered
6. Mobile checklist (from AGENTS.md): navbar, map visible, controls panel max-height, no horizontal overflow

### Phase 4 — Ship prep

1. Squash/reword commits into coherent history
2. PR to `beta` with summary + test plan
3. CHANGELOG v1.6 entry
4. Beta soak → promote to `main` per usual

---

## Verification commands (worktree)

```powershell
cd C:\Users\super\.codex\worktrees\21e4\Graphical-Forecast-Creator

# Unit (monitor-focused)
pnpm test -- --runTestsByPath `
  src/components/Monitor/MonitorControls.test.tsx `
  src/monitor/monitorSettings.test.ts `
  src/monitor/outlookSources.test.ts `
  src/monitor/wms.test.ts

# Dev
pnpm dev
# → http://127.0.0.1:3001/monitor?localBetaBypass=true

# E2E (with dev server running)
$env:PLAYWRIGHT_BASE_URL='http://127.0.0.1:3001'
$env:PLAYWRIGHT_SKIP_WEBSERVER='1'
pnpm exec playwright test e2e/smoke.spec.ts -g monitor
```

---

## Session log

| Date | Notes |
|------|-------|
| 2026-05-21 | Audited worktree; documented state |
| 2026-05-21 | Implemented WMS playback (`useLiveWmsLayers`), tests, committed, rebased onto `beta`; confirmed Monitor is standalone (not Home) |

---

## Open questions (product)

1. **Front page:** Tab only vs Home redesign vs default landing route?
2. **Animation MVP:** Radar only, satellite only, or both + linked clock?
3. **Free tier:** Any live layers without premium, or outlook-only for free?
4. **v1.6 scope cut:** Ship without animation if WMS time loops are high effort?

---

## Related code map (quick reference)

```
/monitor
  MonitorPage.tsx      — orchestration, WMS time fetch, cloud outlook load, sync hooks
  MonitorPage.css      — grid layout, mobile/landscape breakpoints
  monitorSlice.ts      — Redux settings + map view
  monitor/types.ts     — MonitorSettings schema + normalize
  monitor/wms.ts       — NOAA WMS URLs + buildRadarLayerConfig / buildSatelliteLayerConfig
  monitor/outlookSources.ts — current/local/cloud option list
  monitor/storage.ts   — localStorage key
  components/Monitor/
    MonitorControls.tsx — sidebar UI (includes Playback section)
    MonitorMap.tsx       — OpenLayers (no animation yet)
```
